# from flask_user import UserManager, SQLAlchemyAdapter
# import src.user.models as user_models
# from .models import db
#
# db_adapter = SQLAlchemyAdapter(db, user_models.User)
#
# user_manager = UserManager(db_adapter)
